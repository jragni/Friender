import React from 'react';

export default function EditProfile(props) {

    return(
        <form className="EditProfile">
            {/* TODO: add fields when models are built out */}
        </form>
    )
}