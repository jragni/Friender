import React from 'react';

/** Matches
 * The component that contains the list of matches the current user has.
 * 
 * Props:
 * 
 * State:
 * 
 * 
 * Routes -> Matches -> MatchCard
 */
export default function Matches(props) {
    return(
        <div className="Matches">
            {/* TODO: add list of matches */}
            {/* Pagination for match lists */}
        </div>
    )
}